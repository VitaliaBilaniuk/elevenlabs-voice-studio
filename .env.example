# Copy to .env and fill in. .env is gitignored.

# Required. Create one at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=

# Optional. Port the proxy server listens on (default 8787).
API_PORT=8787
